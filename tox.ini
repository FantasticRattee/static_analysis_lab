# cat > tox.ini << 'EOF'
# [tox]
# envlist = py311

# [testenv]
# deps =
#     pytest==7.4.3
#     pytest-cov
# commands =
#     pytest --cov=. --cov-report=xml:coverage.xml tests/
# setenv =
#     PYTHONPATH = {toxinidir}/src
# EOF

[tox]
envlist = py310

[testenv]
deps =
    pytest
    pytest-cov
commands =
    pytest --cov=. --cov-report=xml